# AGENTS.md：在本仓库干活的常驻规则

本仓库是中文「学期课程学习仓库」：课程资料 + 一个自建静态阅读站。本文件每次会话自动加载，
所以只放**每次都要生效**的规则。操作规程见[维护细则](./维护细则.md)，原则见[维护条例](./维护条例.md)
（两者冲突时以条例为准）；进度手记是 `.zcode/handoff.md`（按约定不入库），开工前先读它。

## 一、先挂 skill，再动手

skill 的正文**不会自动加载**——上下文里只有它的名字和一句话描述，必须主动调用 `Skill` 工具才读到正文。
所以触发条件按**状态**写：出现下表左列的情况就自己挂，不必等用户点名，也不要因为「规则我大致记得」跳过。

| 出现这个状态 | 先做什么 |
| :--- | :--- |
| 要往仓库加或改任何内容：写解析、加课程、整理转写、改白名单与索引、提交推送 | `Skill(repo-maintenance)`，并读它指出的两份规程；其中的**反人类解析禁令**与**盲测**是入库硬闸 |
| 要写或改数学解析、作业解答、通关攻略 | `Skill(human-first-solution)`：「试探／凑／巧妙看出」= 没写完，先找机械做法 |
| 一份解析或作业刚入库（用户提没提都要跑） | `Skill(double-sign)`：派独立子代理只拿题面重算 |
| 站点视觉或交互出问题 | 开浏览器实测，不空猜 |
| 用户说「想不到这一层」「怎么试出来的」「AI 味」「双签」「他证」「校对答案」 | 按上表对号入座，必用 |

**同一会话只挂一次**：skill 正文不会变，重复挂每次约 2k token 纯属浪费；每个新会话的第一条内容任务照常先挂。

## 二、硬约束

- **主代理一张图都不看**：批量读图派子代理，每人 ≤6 张；禁止 OCR、禁止装识别工具、禁止裁剪放大增强。
- **`.zcode/` 一律不推送**，只落盘。
- **精确白名单**：`.gitignore` 是 `*` → `!*/` → 逐文件 `!/完整相对路径`，禁通配符；新文件先加白名单再 `git add`。
- **不猜补**：教材题面、数值、下标、口误、缺失板书一律不猜，标「待核对」并写明来源。
- **资料与索引同进同出**：`课程/<课程>/<类别>/` 下任何资料变动，同一次提交必须更新对应 `index.md`。
- **改 `课程/` 或 `站点/` 的提交必须同轮更新 `.zcode/handoff.md`**（pre-commit 强制）。不要用 `--no-verify`。
- 提交前跑：`update-readme.py --worktree`、`check-markdown.py`、`test-update-readme.py`、`git fsck --no-progress`。
- **禁止等 CI**（2026-09-24 用户定案）：本地构建与 CI 构建等效（同一个 `node build.mjs`、同一套钩子、同一批素材），
  **`git push` 就是这一步的终点**——不要 `sleep`、不要轮询、不要为了「看 CI 绿不绿」多等一分钟。
  只有怀疑基础设施本身（比如部署步骤报密钥错）时才偶尔看一眼。
- **入库保持一次过**（2026-09-23 定案）：转写用 `.githooks/extract-transcript.py` 提取，索引只做最小 diff（加行 + `last_updated`），收尾用 skill 里那条命令**一次跑完**；线上页面只在动过 `站点/` 或用户要看时抓。

## 三、写作口径

- 中文优先、给人看；**平实陈述**，不写自辩或防范性说明。
- **图片与动画线已放弃**（2026-09-20 定案）：不要再提，也不要换个说法重新发明。
- 数学内容机械做法优先落笔，巧解只作对照；验算必须独立。
