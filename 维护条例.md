---
id: okf.repo.maintenance.charter
name: 仓库维护条例
type: Concept
spec_version: "0.2"
status: active
last_updated: "2026-09-17"
stewards:
  - team: 工程数学课程学习记录
---

# 仓库维护条例

本条列对仓库任何时期的任何改动都适用；与 README 冲突时以本条例为准并同步修订 README。

## 1. 仓库定位与读者

- 本仓库是给人看的学期学习仓库，不是纯机器格式库。OKF（v0.2）元数据只作轻量结构约束，不牺牲可读性；标题、正文、索引一律中文优先。
- 仓库代表**当前学期**，以中文课程名为主分类；每门课固定五类子目录：作业、教材解析、课堂笔记、原始资料、音频。

## 2. 真实性与溯源（最高优先级）

1. **不猜补**：教材题面、数值、下标、口误、缺失板书一律不猜；看不清就标注"待核对"并写明照片来源，后续重读修正。
2. **区分身份**：教材原文陈述、整理者补充、学生手写旁注三者在解析中明确区分；学生草稿不得当作正确答案直接采信，需独立验算。
3. **来源登记**：每份教材解析注明原书页码与照片来源；原始文件保持字节不变迁入仓库，登记 SHA-256。
4. **不重复维护**：同一内容只在一处保存正文，其余位置用链接；课堂例题的规范化重做放课堂笔记，教材解析只登记题面并互链。
5. **正式作业只认正式来源**：教材习题、课堂例题不自动认定为教师布置作业；题号、截止时间以学习通等正式通知为准。

## 3. 结构与命名

- 课程目录：`课程/<中文课名>/<类别>/`；各类入口为 `index.md`。
- 日期类资料（作业、课堂笔记、音频记录）：`YYYY-MM-DD-名称.md`。
- **教材解析：一节一个文件**，放在章目录 `教材解析/第NN章-章名/` 下，文件名为 `第NN章-<节号>-<节名>.md`；一节内容过多时在该文件内按二级小节继续拆。**不把整章塞进一个 md。**
- 每个教材解析文件内部固定先写**原书抄录**（按原书页码抄录印刷正文与原编号），紧跟**解析**（概念、条件、推导、例题解法、易错点、验算）。抄录与解析同文件、分小节呈现，先抄后析，便于逐句对照。
- 新建课程：复制 `模板/` 下五个模板建立五类 `index.md`，并同步登记课程总索引与课程入口。
- `knowledge/` 是历史兼容入口，只放迁移说明，不再新增正文。

## 4. Git 流程

1. **精确白名单**：`.gitignore` 保持 `*` → `!*/` → 逐文件 `!/完整相对路径`，禁止通配符；每个新文件先加白名单再 `git add`。
2. **hook 必须启用**：克隆后执行 `git config core.hooksPath .githooks`；提交由 pre-commit 生成 README 自动区块，推送由 pre-push 校验待推送提交树。
3. **每次编辑教材解析（或任何资料）后**：运行 `python3 .githooks/update-readme.py --worktree` 同步 README 自动目录，再 `git status` 确认无意外改动。
4. **提交前自检**：`python3 .githooks/test-update-readme.py` 全绿；`git diff --check` 干净；真实暂存区保持为空直到正式提交。
5. 迁移、移动文件必须保持链接有效（hook 会拒绝断链），删除资料时同步更新分类索引。

## 5. 音频与外部材料

- 音频目录只存**下载链接与元数据**（课程、日期、准确课次/节次、主题、链接来源与有效期、关联笔记），不提交音频二进制，不写入任何凭据。
- 课次编号、节次必须由正式资料确认，不得凭文件名或日期推断。

## 6. 验证基线

任何改动后应保持（当前基线，2026-09-17）：12 项集成测试通过；本地链接全部有效；`git diff --check` 通过；README 自动目录与工作区一致；Markdown 美观度检查无错误。

- `python3 .githooks/test-update-readme.py`：目录、白名单、命名与索引的集成测试。
- `python3 .githooks/check-markdown.py`：Markdown 美观度与可读性检查；错误项必须为 0，`--strict` 时警告也失败。检查标题层级、表格列数、代码围栏与数学定界符闭合、行尾空格、空行、超长行等。
- 迁移完整性检查脚本由当次会话在 `/tmp` 运行，不入库。
