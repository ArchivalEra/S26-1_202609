---
id: okf.repo.maintenance.details
name: 仓库维护细则
type: Concept
spec_version: "0.2"
status: active
last_updated: "2026-09-17"
stewards:
  - team: 学期课程学习记录
---

# 仓库维护细则

本细则适用于**任意数量课程**的学习仓库：增加课程、增加资料类别、迁移目录都不需要改写本细则，只需要按下面的固定动作执行。原则性规定见[仓库维护条例](./维护条例.md)；两者冲突时以条例为准，并同步修订本细则。

**与 README 的关系**：README 的课程目录和本文件的「仓库状态」都是**自动区块**，由 `.githooks/hook_lib.py` 在每次提交时从暂存内容重新生成，推送前校验一致性。手工改动会被覆盖；要改内容，改自动区块之外的正文。

## 1. 每次编辑后的固定动作（强制）

任何一次新增、修改、移动或删除资料之后，按顺序执行：

```bash
python3 .githooks/update-readme.py --worktree   # 同步 README 课程目录 + 本文件状态区块
python3 .githooks/check-markdown.py             # Markdown 美观度，错误必须为 0
python3 .githooks/test-update-readme.py         # 目录、白名单、命名、索引集成测试
git fsck --no-progress                          # 仓库对象完整性，不应有输出
git diff --check                                # 空白与冲突标记
```

四项都通过再提交。提交由 pre-commit 再次生成两处自动区块；推送由 pre-push 校验待推送提交。

## 2. 新增一门课程

1. 在 `课程/<中文课名>/` 下按五个固定类别建目录：作业、教材解析、课堂笔记、原始资料、音频。
2. 每类复制 `模板/` 下对应模板生成 `index.md`；课程入口 `课程/<课名>/index.md` 复制 `模板/课程索引.md`。
3. 在 `课程/index.md` 登记这门课；在 `.gitignore` 为**每个新文件**加一行 `!/完整相对路径`。
4. 跑第 1 节的固定动作，然后提交。

不需要改本细则、README 正文或钩子脚本——自动区块会自己反映新课程。

## 3. 教材照片归档与读图

### 3.1 归档

- **原件不入库**：教材照片由拍摄者本地保存，仓库内不存放图片二进制。手机 HEIC 与 JPG 都不入库，也不转格式、不压缩、不重命名。
- 在 `课程/<课名>/原始资料/index.md` 逐张登记**文件名、原书页码/内容与 SHA-256**，并在表格里把文件名写成**纯文本**（不加链接，避免死链）。这是原件的身份凭证。
- 登记后逐张核对：登记哈希 = 拍摄者本地原件哈希。核对命令见[维护条例](./维护条例.md)第 6 节验证基线。
- 需要方向校正、缩放或裁剪来读图时，由 `.githooks/prepare-photos.py` 从**仓库外原件**生成 `/tmp` 阅读副本；副本不入库、不作证据引用。
- 解析正文引用照片时用文件名（如 `IMG20260917203306.jpg`）或页码，不写仓库内路径。

### 3.2 读图（强制交给子代理）

**主对话每对话最多 60 张图片**，超限后主代理无法再读图，会把整批任务卡住。因此：

- **批量读图一律派子代理**：用 Agent 工具（`general-purpose`）分批读，一个子代理读 4–8 张。提示词只列出**要读的具体那几行字**（某个下标、某个标注），并写明「**不要转录整页正文**」。
- **每张图只读一次，看不清就上报重拍**：禁止重复打开同一张、禁止换副本重试、禁止裁剪/放大/增强等自救手段。读不清的按「文件名 + 原书页码 + 看不清的位置 + 原因」报给用户重拍，由用户决定。正常单张约 1 万 token；若某次读图远超此量级，说明提示词里让它抄整页了。
- 子代理有独立的图片预算，不受主对话上限影响；主代理只汇总它返回的文字结论。
- 主代理只在需要目视确认**一两个关键点**（例如某一步变换标注、某个下标）时才直接读图，并计入自己的预算。
- **禁止用 OCR 代替读图**：公式、矩阵、上下标、手写旁注在识别结果里普遍出错（实测会漏掉 `1`、负号和下标）。OCR 只能用来辅助定位页码，不能作为转录依据。
- **子代理只许用 Read 看图，不许自建识别管线**：实测有子代理收到「读照片」指令后，自行建虚拟环境、装识别工具、把图旋转缩放截图做探针，整轮作废且结果一字不可用。派活提示词必须写死三条：
  - 只允许 Read；禁止使用或安装任何 OCR／文字识别工具与模型，禁止 `pip`/`uv`/`apt` 安装；
  - 禁止对图片做旋转、裁剪、缩放、增强、拼接；Read 打不开或看不清就**立即停下如实报告**，不许另找替代方案；
  - 报告开头必须先列「实际打开的文件清单＋未能打开的文件」，没有这份声明的结果一律丢弃重派。
- 主代理派活时写清：这是第几章第几节、期望的页码区间、需要逐字核对的记号（箭头上的变换标注、下标、符号），并要求区分「数值确定」与「标注看不清」。

### 3.3 教材解析写作（先抄后析）

- **一节一个文件**，放在 `课程/<课名>/教材解析/第NN章-章名/` 下，命名 `第NN章-<节号>-<节名>.md`；不把整章塞进一个 md。
- 每个文件内部固定：先**原书抄录**（按原书页码抄录印刷正文与原编号，公式用 LaTeX 重排），紧跟**解析**（概念、条件、推导、例题解法、易错点、验算）。
- 教材陈述与整理者补充必须分开标注；学生手写答案不得当作标准答案；教材习题不自动认定为教师布置的作业。
- 例题与习题的数值结果要用**精确算术独立验算**（有理数或整数运算），不照抄书上手写过程；验算发现的差异要写进解析并在文件末尾说明。
- 每个文件末尾写「本节完成边界」：已抄录、已解析、〔待核对〕、未完成，不夸大进度。

### 3.4 资料与索引同进同出（hook 强制）

`课程/<课程>/<类别>/` 下任何资料（不含 `index.md`）**新增、删除或修改**，同一次提交必须同时改对应类别的 `index.md`（含其中 `last_updated`），否则 pre-commit 直接拒绝。索引是资料的登记处；资料变了索引不动，索引就已过期。作业记录照此办理：新增作业要登记进 `作业/index.md`，改正文要同步索引里的状态与时间。

## 4. 命名与目录速查

| 类别 | 位置 | 命名 |
| :--- | :--- | :--- |
| 作业 | `课程/<课名>/作业/` | `YYYY-MM-DD-名称.md` |
| 教材解析 | `课程/<课名>/教材解析/第NN章-章名/` | `第NN章-<节号>-<节名>.md` |
| 课堂笔记 | `课程/<课名>/课堂笔记/` | `YYYY-MM-DD-名称.md` |
| 原始资料 | `课程/<课名>/原始资料/` | 保留原文件名，不强制日期 |
| 音频记录 | `课程/<课名>/音频/` | `YYYY-MM-DD-名称.md` |

各类目录入口一律 `index.md`；`knowledge/` 只作历史兼容，不新增正文。

## 5. 白名单与提交

- `.gitignore` 保持 `*` → `!*/` → 逐文件 `!/完整相对路径`，**禁止通配符**；新增文件先加白名单。
- 提交信息用中文，说明做了什么；不提交音频二进制、不写入任何凭据。
- 每次提交后确认工作区干净；推送前确认 pre-push 通过。

## 6. 禁止事项

1. 猜补看不清的题面、数值、下标；拿不准就标〔待核对〕并写明照片来源。
2. 把 OCR 结果、转换后的临时图、学生手写答案当作权威依据。
3. 用子代理的转录直接成文而不做数值验算；转录只解决「书上写了什么」，不解决「写得对不对」。
4. 只在一个位置维护正文，其他位置用链接；不复制第二份。
5. 凭文件名或日期推断课次、节次；课次以正式资料为准。

## 7. 故障处理

- **`git fsck` 报对象损坏**：先确认工作区文件与源文件哈希一致，再删除损坏的松散对象（`.git/objects/xx/…`）并重新 `git add` 该文件，最后重跑 `git fsck`。不要用 `git gc` 掩盖问题。
- **提交被 pre-commit 拦下**：按提示修好命名、索引链接或自动区块，不要用 `--no-verify` 绕过；该选项只应作为临时诊断手段，且推送端仍会校验。
- **主对话读图超限**：改为派子代理读，或把剩余张数留到下一轮对话继续。

<!-- AUTO-MAINTENANCE:START -->

## 仓库状态（自动生成）

> 本区块由 `.githooks/hook_lib.py` 在**每次提交时从暂存内容重新生成**，推送前校验一致性；手工修改会在下次提交时被覆盖。要改内容请改这一节之外的正文。

- 课程 **1** 门；正式资料 **16** 份（不含索引、模板和历史入口）。
- 精确白名单 **108** 条逐文件路径（无通配符），仓库共 **108** 个受管文件。
- 暂存内容摘要（不含本文件与 README）：`96b9a45679bf44a59979e834fddfa36ec594b63eba6dbbde20e0749c81722180`。任何正文改动都会改变这一行。

### 课程分类一览

| 课程 | 作业 | 教材解析 | 课堂笔记 | 原始资料 | 音频 | 五类索引 |
| :--- | ---: | ---: | ---: | ---: | ---: | :--- |
| 工程数学 | 1 | 12 | 2 | 1 | 0 | 齐全 |

### 教材解析进度

**工程数学**（12 个文件）

- `课程/工程数学/教材解析/0.0-符号入门.md` — 符号入门
- `课程/工程数学/教材解析/第一部分-线性代数/第1章-行列式/1.0-本章通关攻略.md` — 第 1 章 行列式 · 通关攻略
- `课程/工程数学/教材解析/第一部分-线性代数/第1章-行列式/1.1-二阶与三阶行列式.md` — 第1章 1.1 二阶、三阶行列式（原书 p1–p5）
- `课程/工程数学/教材解析/第一部分-线性代数/第1章-行列式/1.2-n阶行列式.md` — 第1章 1.2 n 阶行列式（原书 p6–p15）
- `课程/工程数学/教材解析/第一部分-线性代数/第1章-行列式/1.3-克莱姆法则.md` — 第1章 1.3 克莱姆法则（原书 p15–p20）
- `课程/工程数学/教材解析/第一部分-线性代数/第1章-行列式/1.4-用MATLAB计算行列式.md` — 第1章 1.4 用 MATLAB 进行行列式的计算（原书 p20–p21）
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.0-本章通关攻略.md` — 第 2 章 矩阵 · 通关攻略
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.1-矩阵的概念及运算.md` — 第2章 2.1 矩阵的概念及运算（原书 p22–p33）
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.2-逆矩阵.md` — 第2章 2.2 逆矩阵（原书 p33–p38）
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.3-矩阵的初等变换与初等矩阵.md` — 第2章 2.3 矩阵的初等变换与初等矩阵（原书 p38–p43）
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.4-矩阵的秩.md` — 第2章 2.4 矩阵的秩（原书 p43–p46）
- `课程/工程数学/教材解析/第一部分-线性代数/第2章-矩阵/2.5-用MATLAB进行矩阵运算.md` — 第2章 2.5 用 MATLAB 进行矩阵运算（原书 p46–p51）

### 原始资料构成

| 课程 | `.doc` |
| :--- | ---: |
| 工程数学 | 1 |

### 待补提示（自动列出，非错误）

- 工程数学：音频目录尚无记录，待正式通知或链接，登记前不得编造。

### 验证基线

- `python3 .githooks/update-readme.py --worktree`：同步 README 与维护细则自动区块。
- `python3 .githooks/test-update-readme.py`：目录、白名单、命名、索引与移动的集成测试。
- `python3 .githooks/check-markdown.py`：Markdown 美观度与可读性检查，错误项必须为 0。
- `git fsck --no-progress`：仓库对象完整性，不应有输出。
- 推送由 `.githooks/pre-push` 校验待推送提交的两处自动区块。

<!-- AUTO-MAINTENANCE:END -->
